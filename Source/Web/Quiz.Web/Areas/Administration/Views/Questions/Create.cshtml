@model Quiz.Web.Areas.Administration.ViewModels.Questions.QuestionInputModel

@using (Html.BeginForm("Create", "Questions", new { area = Quiz.Common.GlobalConstants.AdminArea }, FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.ValidationSummary(true, string.Empty, new { @class = "text-danger" })
        <div class="row container">
            <div class="col-md-4">
                @*@Html.EditorFor(m => m.Name)
                @Html.EditorFor(m => m.WebAddress)
                @Html.EditorFor(m => m.PhoneNumber)
                @Html.EditorFor(m => m.EMail)
                @Html.EditorFor(m => m.GoogleMapsUrl)*@
                <div class="form-group">
                    <div>@Html.LabelFor(m => m.selectedAuthors)</div>
                    @Html.ListBoxFor(m => m.selectedAuthors, Model.AuthorList, new { @class = "form-control chzn-select", @data_placeholder = "Choose Movies" })
                </div>
                <div class="form-group">
                    <div>@Html.LabelFor(m => m.QuoteId)</div>
                    @Html.DropDownListFor(m => m.QuoteId, Model.QuoteList, new { @class = "form-control chzn-select", @data_placeholder = "Choose City" })
                </div>
            </div>
            @*<div class="col-md-6">
                @Html.EditorFor(m => m.Address)
            </div>*@
        </div>
        <div class="row container">
            <div class="col-md-12">
                <div class="form-group">
                    @Html.Submit(new { value = "Save", @class = "btn btn-primary" })
                </div>
            </div>
        </div>
    </div>
}

@*@section scripts{
    @Scripts.Render("~/bundles/activate-chosen")
    @Scripts.Render("~/bundles/jqueryval")
}*@


